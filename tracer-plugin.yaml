data:
  components:
    rpms:
      api:
      dependencies: false
      packages:
  dependencies:
    buildrequires: {tracer: 12-1}
  description: Module that depends on tracer module
  license:
    module: [MIT]
  name: tracer-plugin
  profiles:
    default:
  references: {community: 'http://www.example.com/', documentation: 'http://www.example.com/',
    tracker: 'http://www.example.com/'}
  release: '1'
  summary: A module with dependency on tracer module
  version: 1
document: modulemd
version: 0
